# 22.3 - DynamoDB Basics - Hands On

> Hands-on walkthrough. Core concepts (partition key, sort key, flexible schema, table classes, hot partitions) already covered in 22.2.

## New Info

### Capacity Modes (preview — covered in depth in 22.4)
- **Provisioned**: you set fixed RCU/WCU — has a **free tier**
- **On-Demand**: scales automatically, pay per request — no free tier

### Encryption at Rest
- Default: DynamoDB-managed key
- Other options available (KMS customer-managed keys)
